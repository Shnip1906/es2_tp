@model List<SkillsProfModel>?
@{
    ViewBag.Title = "Skills";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h2 class="text-center"> Lista das Skills</h2>  
<a class="btn btn-primary" asp-area="" asp-controller="Perfil" asp-action="CriarSkillPerfil">Adicionar Skill</a>
<table class="table">
    <thead>
    <tr>
        <th>
            Número Horas
        </th>
        <th>
            Skill
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nhoras)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.NomeSkills)
            </td>
     </tr>
    }
    </tbody>
</table>
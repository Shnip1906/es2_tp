@using Microsoft.AspNetCore.Http
@model IEnumerable<BusinessLogic.Entities.Skillspropostum>
@{
    ViewBag.Title = "Skills";
    Layout = "~/Views/Shared/_Layout5.cshtml";
}

<h2 class="text-center"> Lista das skills</h2>
<p>
    <a class="btn btn-primary" asp-area="" asp-action="AdicionarSkills" asp-route-id="@ViewBag.Id">Adicionar Skills</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            Skills
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdSkillsNavigation.NomeSkills)
            </td>

            <td>
                <a asp-action="EliminarSkillProposta" asp-route-id="@item.IdSkillsProposta">Eliminar</a>
            </td>
        </tr>
    }
    </tbody>
</table>
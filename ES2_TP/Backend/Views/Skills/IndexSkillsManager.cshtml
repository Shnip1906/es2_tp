@using Microsoft.AspNetCore.Http
@model List<SkillsModel>?
@{
    ViewBag.Title = "Skills";
    Layout = "~/Views/Shared/_Layout4.cshtml";
}
<div>
    <h2 class="text-center">Lista das Skills</h2>
    <a class="btn btn-primary" asp-area="" asp-action="CreateUM">Criar nova Skill</a>
</div>
<table class="table">
    <thead>
    <tr>
        <th> 
            Skills
        </th>
        <th>
            Área Profissional
        </th>
  
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeSkills)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeAreaProfissional);
            </td>
          
        </tr>
    }
    </tbody>
</table>
